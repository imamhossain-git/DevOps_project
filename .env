# MongoDB Atlas Connection Strings
# Replace with your actual MongoDB Atlas connection strings
# For testing, you can use these placeholder URLs or create your own free cluster

# Example format (replace with your actual credentials):
# MONGODB_URI_PRODUCT=mongodb+srv://username:password@your-cluster.mongodb.net/product_db?retryWrites=true&w=majority&appName=e-commerce
# MONGODB_URI_ORDER=mongodb+srv://username:password@your-cluster.mongodb.net/order_db?retryWrites=true&w=majority&appName=e-commerce

# For local testing without MongoDB Atlas, you can use these fallback URLs:
MONGODB_URI_PRODUCT=${MONGODB_URI_PRODUCT:-mongodb://localhost:27017/product_db}
MONGODB_URI_ORDER=${MONGODB_URI_ORDER:-mongodb://localhost:27017/order_db}